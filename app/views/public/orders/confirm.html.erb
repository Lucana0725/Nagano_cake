<!--<h1>Public::Orders#confirm</h1>-->
<!--<p>Find me in app/views/public/orders/check.html.erb</p>-->
<h2>注文情報確認</h2>
<table>
  <thead>
    <tr>
      <th>商品名</th>
      <th>単価(税込)</th>
      <th>数量</th>
      <th>小計</th>
    </tr>
    <tbody>
      <% @cart_items.each do |cart_item| %>
      <% @total_price += cart_item.get_subtotal_price %>  <!--cart_item/indexで使用した@total_price += cart_item.get_subtotal_priceを再利用。"cart_item.get~"は単一のカート商品にのみしか適用されないので、eachの中に入れて回してあげる-->
      <tr>
        <td><%= cart_item.item.name %></td>  <!--CartItemはItemとアソシエーションを結んでいるので、カート内商品の各情報が欲しければ.itemとする必要がある-->
        <td><%= cart_item.item.get_tax_in_price %></td>
        <td><%= cart_item.amount %></td>  <!--amountはCartItemで定義しているので.itemとする必要はない。-->
        <td><%= cart_item.get_subtotal_price %></td>  <!--amountと同様-->
      </tr>
      <% end %>
    </tbody>
  </thead>
</table>
<table>
  <tr>
    <th>送料</th>
    <td><%= @order.show_postage %></td>
  </tr>
  <tr>
    <th>商品合計</th>
    <td><%= @total_price %></td>  <!--上のeachで回した@total_priceを表示-->
  </tr>
  <tr>
    <th>請求金額</th>
    <td><%= @order.show_postage + @total_price %></td>
  </tr>
</table>

<p>注文を確定する</p>