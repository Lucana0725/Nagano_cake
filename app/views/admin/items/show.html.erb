<div class="container pt-3">
  <div class="row">
    <div class="bg-light">
      <h3>商品詳細</h3>
    </div>
  </div>

  <div class="row pt-5">
    <div class="col-4">
      <%= image_tag @item.get_item_image(300, 300) %>
    </div>

    <!--<p>商品名 %= @item.name %></p>-->
    <!--<P>商品説明 %= @item.introduction %></P>-->
    <!--<p>ジャンル %= @item.genre.name %></p>-->
    <!--Itemモデルで定義した消費税込み価格を表示するメソッドを使用-->
    <!--<p>税込み価格 %= @item.get_tax_in_price.to_s(:delimited) %> (%= @item.price.to_s(:delimited) %>) 円</p>-->

    <div class="col-8">
      <table class="table table-borderless">
        <tr>
            <th>商品名</th>
            <td><%= @item.name %></td>
        </tr>
        <tr>
            <th>商品説明</th>
            <td><%= @item.introduction %></td>
        </tr>
        <tr>
            <th>ジャンル</th>
            <td><%= @item.genre.name %></td>
        </tr>
        <tr>
            <!--Itemモデルで定義した消費税込み価格を表示するメソッドを使用-->
            <th>税込価格</th>
            <td><%= @item.get_tax_in_price.to_s(:delimited) %> (<%= @item.price.to_s(:delimited) %>) 円</td>
        </tr>
        <tr>
          <th>販売ステータス</th>
          <% if @item.is_active %>
            <td class="text-success">
              販売中
            </td>
          <% else %>
            <td class="text-secondary">
              販売停止中
            </td>
          <% end %>
        </tr>
        <tr>
          <td></td>
          <td>
            <%= link_to "編集する", edit_admin_item_path(@item.id), class: "btn btn-success" %>
          </td>
        </tr>
      </table>

    </div>

  </div>
  <!--items_controllerで定義した@itemに、画像表示のget_item_imageと以下表示させたいカラムを渡す-->
</div>